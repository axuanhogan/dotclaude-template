---
allowed-tools: Bash(git add *), Bash(git commit *), Bash(git status *)
description: Git commit changes to the repository
model: haiku
---

Create a git commit with the appropriate message based on the changes.